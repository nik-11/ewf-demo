## Getting Started

First, build the docker image:

```bash
npm run build
```

Then, start the container in detached mode:

```bash
npm run dev
```

To stop/remove the container:

```bash
docker compose down
```
